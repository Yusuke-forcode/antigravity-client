# Android（スマホ）からAntigravityを使うための手順書

この手順書では、PC上で起動した `antigravity-client` の Web UI に、外出先の Android スマートフォンから安全にアクセスする方法を解説します。

Tailscale（無料のVPNサービス）を利用することで、ルーターのポート開放などの複雑な設定なしに、スマホからPCへ直接アクセスできるようになります。

---

## 準備するもの

1. **ホストPC** (Antigravity とこのリポジトリがインストールされている Mac または Linux)
2. **Android スマートフォン**
3. **Tailscale アカウント** (無料で作成可能)

---

## Step 1: Tailscale のセットアップ

まず、PCとスマホを同じ仮想プライベートネットワーク（VPN）に参加させます。

### 1-A: PC側の設定
1. [Tailscale公式ページ](https://tailscale.com/) からアカウントを作成します。
2. ホストPCに Tailscale をインストールし、ログインします。
3. インストール後、Tailscaleアプリの設定画面やターミナル（`tailscale ip`）から、**PCのTailscale IPアドレス（`100.x.x.x` など）**をメモしておきます。

### 1-B: Android側の設定
1. Google Playストアから [Tailscale アプリ](https://play.google.com/store/apps/details?id=com.tailscale.ipn) をインストールします。
2. PCと同じアカウントでログインします。
3. 左上のスイッチをオンにして、VPN接続を有効にします。

---

## Step 2: PCで Antigravity サーバーを起動する

PC側で、このリポジトリのWeb UIサーバー（`web-poc`）を起動します。

1. ターミナルを開き、`antigravity-client` のディレクトリに移動します。
2. 以下のコマンドを実行してサーバーを起動します。

```bash
npm run web
```

起動が成功すると、ターミナルに以下のようなメッセージが表示されます。

```
============================================================
  本家UI (web):  https://localhost:8765/   (HTTP/2)
  Upstream LS:    https://127.0.0.1:xxxxx
============================================================
```
※ このサーバーはデフォルトで `0.0.0.0` （PCのすべてのネットワークインターフェース）でアクセスを待ち受けるため、Tailscale経由でのアクセスが自動的に可能になります。

---

## Step 3: Android からアクセスする

1. AndroidスマホでChromeなどのブラウザを開きます。
2. URLバーに以下のように入力してアクセスします。
   （※ `100.x.x.x` の部分は、Step 1-Aでメモした**PCのTailscale IPアドレス**に置き換えてください）

   ```
   https://100.x.x.x:8765/
   ```

3. **証明書の警告画面について**
   - このサーバーは自己署名証明書を使用しているため、ブラウザで「接続はプライベートではありません」「安全ではありません」といった警告画面が表示されます。
   - Chromeの場合は、**「詳細設定」**をタップし、**「100.x.x.x にアクセスする（安全ではありません）」**をタップして続行してください。

4. 画面が読み込まれ、AntigravityのUIが表示されれば成功です！

---

## ⚠️ 注意事項と運用上のアドバイス

- **自己署名証明書について:** HTTP/2通信を成立させるために自己署名証明書を利用しています。Tailscale経由でのアクセスであれば通信自体は暗号化されているため安全です。
- **PCのスリープ設定:** サーバーを動かしているホストPCがスリープ状態になるとアクセスできなくなります。外出先から使う場合は、PCのスリープをオフにしておくか、画面オフ状態でもバックグラウンド処理が継続する設定にしてください。
- **コンフリクトに注意:** 本家のAntigravity IDE（VS Code等）をPC上で同時に開いていると、データが競合する恐れがあります。スマホからアクセスする際は、PC上のAntigravity IDEは閉じておくことを推奨します。隔離環境で起動したい場合は以下のコマンドを使用してください。
  ```bash
  npm run web -- /あなたの/プロジェクト/パス /tmp/gemini_iso antigravity_client
  ```