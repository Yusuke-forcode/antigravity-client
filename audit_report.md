# セキュリティ・動作監査レポート (Antigravity Client)

本レポートは、`antigravity-client` リポジトリのソースコード全体に対する深層監査の結果をまとめたものです。特に「PCへの致命的な損害」「個人情報やパスワードの窃取（データ流出）」「プロンプトインジェクションや悪意のある挙動」という観点で精査を行いました。

---

## 1. 総合評価と結論

**結論として、本リポジトリに悪意のあるバックドア、パスワード窃取などのデータ流出（エクスフィルトレーション）機能、または作成者によって意図的に仕込まれた悪意のあるプロンプトインジェクションは【存在しません】。**

このツールは、ローカルにインストールされている本家 Antigravity Language Server のバイナリプロセス（`language_server`）をVS CodeなどのIDEなしで起動し、そのプロセスに対してgRPC（ConnectRPC）経由で通信を仲介するための純粋なラッパー／プロキシとして機能しています。

しかしながら、**AIエージェントの性質上、運用方法によってはPCに損害を与える「リスク」は内在しています。**

---

## 2. 懸念事項の評価と詳細

発見された懸念点と、その危険度を10点満点で評価します。

### 懸念 1: 自動実行（Auto-Run）によるシェルコマンド・ファイル操作の暴走
**【懸念度】: 8 / 10**

- **内容:** 本ツールはLLMに対してローカルPCのターミナル操作権限（`run_command`）やファイル書き込み権限（`write_to_file`）を提供します。`EventParser` の実装（`src/core/cascade/event-parser.ts`）において、ユーザーが `allow_auto_run_commands` などの自動実行フラグを有効にした場合、AIは**ユーザーの承認なしに任意のコマンドを実行可能**になります。
- **リスク:** 悪意のあるWebサイトのコードをAIに解析させた場合などに、間接的なプロンプトインジェクションが発生し、AIが誤って `rm -rf /` のような破壊的コマンドを実行したり、機密ファイルを外部に送信する `curl` コマンドを実行する危険性が理論上存在します。
- **対策:** ツール自体の悪意ではありませんが、運用上、ターミナルコマンドの実行前には必ず「承認」を挟むデフォルトの挙動（インタラクションイベントの監視）を維持することが極めて重要です。

### 懸念 2: Tailscale等のVPN利用時のネットワーク露出（リモート実行リスク）
**【懸念度】: 6 / 10**

- **内容:** Web UIを提供する `src/server/web-poc/server.ts` は、デフォルトで `0.0.0.0` （全てのネットワークインターフェース）でリッスンします。TailscaleなどのVPNを利用してスマホからアクセスできるのはこの恩恵ですが、同時に「同じVPNネットワーク上にいる他の端末」からもアクセス可能になります。
- **リスク:** ネットワークの設定を誤ると、第三者がブラウザからあなたのPCのAntigravityにアクセスし、ターミナルを操作される（リモートコード実行される）恐れがあります。

### 懸念 3: プロンプトインジェクションや非可視化された難読化コードの有無
**【懸念度】: 1 / 10 (無害)**

- **内容:** リポジトリ内（特に `src/gen/exa/` や `src/proto_generated/`）に `system_prompt` や `use_antigravity_as_browser_prompting`, `continue_after_injection` などの文字列が大量に見つかります。また `resource/` フォルダには巨大なJavaScriptファイルがあります。
- **リスク評価:** これらは**全て無害**です。
  - プロトコル定義（Protobuf）の中にある「prompt」や「injection」という言葉は、本家のAntigravityシステムがコンテキストを注入したり、プロンプトを管理するための正規のAPI仕様（スキーマ）をそのまま出力したものです。このツールの作者が意図的に仕込んだLLMへの洗脳プロンプトではありません。
  - `resource/` 内のスクリプトは、ブラウザ環境で本家UIを動かすためのパッチ（Electron環境のShim）であり、悪意のある非可視化コード（パスワード窃取など）は含まれていません。

### 懸念 4: 認証情報（APIキー等）のデータ流出（窃取）リスク
**【懸念度】: 0 / 10 (安全)**

- **内容:** `src/server/auth-reader.ts` にて、PC上の `state.vscdb` （VS CodeのSQLiteデータベース）からAntigravityのOAuthトークンやAPIキーを読み取る処理があります。
- **リスク評価:** 監査の結果、読み取ったAPIキーは**ローカルで起動したLanguage Serverバイナリ（127.0.0.1）に通信を許可するためのローカル認証ヘッダとしてのみ使用**されています。外部の第三者サーバー（作者のサーバーなど）に `fetch` や `http.request` 等でデータを送信するコードは一切存在しません。Googleの公式エンドポイントにのみ直接通信されます。

---

## 3. 今後の運用と必要なスキル・パッケージ

このリポジトリを使用していく上で、以下の点に注意が必要です。

1. **特殊なスキルの必要性 (リバースエンジニアリングの脆さ)**
   このツールは本家Antigravityの非公開な通信規約（gRPC）をリバースエンジニアリングして成り立っています。本家がアップデートされ通信形式が変更された瞬間、**このツールは確実に壊れます。** これを直し続けるには、通信を解析して `Protobuf` を再生成するという極めて高度な特殊スキルが必要です。永続的に使えるツールではないという前提で利用してください。

2. **特殊なパッケージのインストール**
   基本的には `Node.js` (npm) の標準的な依存パッケージのみで動作しますが、証明書生成や通信制御に関連するモジュールが含まれています。監査時点でのパッケージ（`package.json`）に不審なマルウェアパッケージは含まれていません。
   （※Windowsで実行する場合は、ネイティブモジュールやC++ビルドツールのトラブルを避けるため、WSL2環境のUbuntu上で実行することを強く推奨します）。

---
**総括:**
リポジトリ自体のコードはクリーンであり、情報の窃取を行うような悪意のあるプログラムではありません。ただし、「AIにPCを操作させる」という強力な基盤であるため、自動実行（Auto-Run）の扱いと、ネットワークのアクセス制限（VPNのルーティング等）には自己責任で厳重に注意を払って使用してください。